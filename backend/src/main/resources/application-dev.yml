cors:
  allowed-origins: http://localhost:5173,http://localhost:3000

spring:
  datasource:
    url: jdbc:h2:mem:familyhub;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  flyway:
    enabled: false